
---
// src/pages/[...locale]/sound.astro
import "@/styles/global.css";
import BaseLayout from "@/layouts/BaseLayout.astro";
import PageLayout from "@/layouts/PageLayout.astro";
import { defaultLang, locales } from "@/../i18n.config";
import { getI18nInfo } from "@/utils/i18n";
import { getShortSoundCloudTracksLinks } from "@/utils/getSoundCloudTracksLinks";
import SoundCloudPlayer from "@/components/SoundCloudPlayer.astro";

// Genera rutas estáticas: "/", "/en"
export function getStaticPaths() {
  return locales.map((lang) => {
    if (lang === defaultLang) {
      return { params: { locale: undefined } };
    }
    return { params: { locale: lang } };
  });
}

const { dictionary, langParam } = await getI18nInfo(Astro.params.locale);
const { soundPage } = dictionary;

const tracksLinks = await getShortSoundCloudTracksLinks();
---

<BaseLayout
  title={soundPage.title}
  lang={langParam}
  description={soundPage.description}
>
  <PageLayout title={soundPage.title} phrase={soundPage.heading}>
    <div class="flex min-h-screen w-full flex-col overflow-y-auto md:flex-col">
      <h1>{soundPage.heading}</h1>
      {
        tracksLinks?.map((link) => {
          return <SoundCloudPlayer trackUrl={link} />;
        })
      }
    </div>

    <!-- Inicio: (Foto impactante + Frase ganadora").

Discografía: (Sus mejores trabajos).

Servicios: (Qué ofrece y, opcionalmente, el "Antes y Después").

Tienda / Beats: (Venta directa).

Bio: (Su historia). -->
  </PageLayout>
</BaseLayout>

<!-- 1. El "Reel" de Audio (La prueba de fuego)Para un ingeniero, cómo suena es más importante que qué música es.Comparativa A/B (Vital): Al igual que con el productor, pero aquí el enfoque es técnico.Mezcla: Raw tracks vs. Final Mix.Mastering: Mix vs. Master (con volumen igualado para que no sea solo "más fuerte", sino "mejor").Restauración: Audio con ruido vs. Audio limpio (muy valorado en podcast y cine).Playlists por Género: Demuestra versatilidad. Una lista de "Rock" (baterías potentes) y otra de "Jazz" (dinámica natural).

 2. Lista de Créditos Técnica (Discografía detallada)A diferencia del productor que pone la portada grande, el técnico necesita ser específico con su rol. Usa una Tabla o una lista limpia:Artista / ProyectoCanción / ÁlbumRol DesempeñadoAñoBanda X"Título"Grabación y Mezcla2023Cortometraje Y"Título"Diseño Sonoro y Foley2024Artista Z"Live Session"Técnico de Monitores2022

 3. Especialización: Sonido en Vivo (FOH / Monitores)Si el técnico trabaja en directos, el audio no se puede mostrar tan fácilmente, así que debes vender la experiencia y la fiabilidad.Lista de Tours/Venues: Artistas con los que ha girado, festivales donde ha operado y salas emblemáticas.Fotos en acción: Fotos de alta calidad operando consolas grandes (DiGiCo, Avid, Yamaha). La imagen del técnico concentrado frente a la mesa de mezclas da mucha autoridad.Conocimiento de Sistemas: Mencionar si sabe ajustar sistemas de PA (L-Acoustics, d&b, Meyer Sound) o usar software de predicción (Soundvision, ArrayCalc).

 4. Especialización: Post-Producción (Cine, TV, Videojuegos)Si se dedica al audiovisual, esta sección es obligatoria.Demo Reel Visual: Un video de 1-2 minutos con fragmentos de trabajos donde haya hecho diseño sonoro, edición de diálogos o mezcla 5.1/Dolby Atmos.Mencionar estándares: Indicar que sabe trabajar bajo estándares de Loudness para TV/Netflix (ej. EBU R128, -23 LUFS, -14 LUFS).

 5. Habilidades Técnicas (Hard Skills)Un técnico es contratado por saber usar herramientas que otros no saben.Software (DAWs): Pro Tools (el estándar), Logic Pro, Reaper, Ableton Live, Nuendo.Certificaciones: Si tiene títulos oficiales (Avid Certified User, Dante Certification, etc.), van aquí con sus logos.Idiomas: Fundamental si quiere trabajar en giras internacionales.
 -->
